# Data dictionary

Generated by `python scripts/build_dictionary.py` from SQL DDL.
Types, nullability, and constraints below describe SQL Server. SQLite stages all CSV cells
as nullable TEXT for validation; its implemented core/mart subset is listed in
[architecture](architecture.md). Blank optional CSV cells become SQL NULL.

The Python field contract also requires unique natural keys, controlled vocabularies,
matching contact authors, consistent lifecycle dates/states, and reconciled exports.

## stg.requests

[SQL declaration](../sql/azure_sql/01_staging_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `request_id` | `varchar(20)` | No | Stable request natural key; one lifecycle fact per request. |
| `workflow_type` | `varchar(40)` | No | Workflow code: authorship-change or copublication. |
| `review_title` | `nvarchar(300)` | No | Invented display title; not a publication title. |
| `review_identifier` | `varchar(30)` | No | Synthetic local review key (SYN-REVIEW-NNNN). |
| `contact_author_name` | `nvarchar(200)` | No | Name of the single author row designated contact author. |
| `created_date` | `date` | No | Request creation day, ISO date in CSV. |
| `due_date` | `date` | No | Request deadline, on or after creation. |
| `current_status` | `varchar(40)` | No | Latest request lifecycle state; must match the last request event. |
| `completion_date` | `date` | Yes | Date of the latest generated/submitted/archived state, when applicable. |
| `dashboard_export_status` | `varchar(40)` | No | Source export state for this request. |

Declared constraints:

- `CONSTRAINT pk_stg_requests PRIMARY KEY CLUSTERED (request_id)`

## stg.authors

[SQL declaration](../sql/azure_sql/01_staging_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `author_approval_id` | `varchar(30)` | No | Request-specific author approval natural key; not a global person ID. |
| `request_id` | `varchar(20)` | No | Stable request natural key; one lifecycle fact per request. |
| `author_name` | `nvarchar(200)` | No | Invented author display label. |
| `author_role` | `varchar(40)` | No | contact author or co-author; exactly one contact per request. |
| `display_order` | `int` | No | Positive author ordering position within the request. |
| `approval_status` | `varchar(40)` | No | pending, sent, approved, declined, or needs-follow-up. |
| `date_sent` | `date` | Yes | Day approval was requested; blank before sending. |
| `date_approved` | `date` | Yes | Day the author approved; present iff author state is approved. |
| `last_reminder_sent` | `date` | Yes | Most recent reminder day, if any. |
| `reminder_due` | `date` | Yes | Planned next reminder day, if any. |

Declared constraints:

- `CONSTRAINT pk_stg_authors PRIMARY KEY CLUSTERED (author_approval_id)`

## stg.approval_events

[SQL declaration](../sql/azure_sql/01_staging_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `event_id` | `varchar(40)` | No | Stable lifecycle or author event natural key. |
| `request_id` | `varchar(20)` | No | Stable request natural key; one lifecycle fact per request. |
| `author_approval_id` | `varchar(30)` | Yes | Request-specific author approval natural key; not a global person ID. |
| `event_type` | `varchar(60)` | No | Request state for request events; author-sent or author-approved for author events. |
| `event_timestamp` | `datetime2(0)` | No | Event time, timezone-naive ISO timestamp (seconds) in the synthetic fixture. |
| `actor_role` | `varchar(60)` | No | Role responsible for the synthetic event. |
| `approval_method` | `varchar(60)` | Yes | Attestation mechanism for approval events, otherwise blank. |
| `comments` | `nvarchar(1000)` | Yes | Generated event annotation; contains no source document text. |

Declared constraints:

- `CONSTRAINT pk_stg_approval_events PRIMARY KEY CLUSTERED (event_id)`

## stg.reminder_events

[SQL declaration](../sql/azure_sql/01_staging_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `reminder_id` | `varchar(40)` | No | Stable reminder natural key. |
| `request_id` | `varchar(20)` | No | Stable request natural key; one lifecycle fact per request. |
| `author_approval_id` | `varchar(30)` | No | Request-specific author approval natural key; not a global person ID. |
| `reminder_type` | `varchar(40)` | No | Synthetic reminder category (follow-up). |
| `sent_at` | `date` | Yes | Reminder send day; no actual message is sent by this project. |
| `sent_by` | `varchar(100)` | No | Synthetic sending actor label. |
| `reminder_number` | `int` | No | Positive ordinal reminder number. |
| `delivery_status` | `varchar(40)` | No | Synthetic delivery state. |
| `next_reminder_due` | `date` | Yes | Planned follow-up day, if any. |

Declared constraints:

- `CONSTRAINT pk_stg_reminder_events PRIMARY KEY CLUSTERED (reminder_id)`

## stg.generated_documents

[SQL declaration](../sql/azure_sql/01_staging_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `document_id` | `varchar(30)` | No | Stable generated-document metadata key; no document body is stored. |
| `request_id` | `varchar(20)` | No | Stable request natural key; one lifecycle fact per request. |
| `workflow_type` | `varchar(40)` | No | Workflow code: authorship-change or copublication. |
| `template_version` | `varchar(30)` | No | Synthetic template version label. |
| `generated_at` | `datetime2(0)` | No | Document generation event date in the fixture; SQL supports a timestamp. |
| `generated_by` | `varchar(100)` | No | Synthetic generating actor label. |
| `document_status` | `varchar(40)` | No | Synthetic output state (needs-review or final). |

Declared constraints:

- `CONSTRAINT pk_stg_generated_documents PRIMARY KEY CLUSTERED (document_id)`

## stg.dashboard_exports

[SQL declaration](../sql/azure_sql/01_staging_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `request_id` | `varchar(20)` | No | Stable request natural key; one lifecycle fact per request. |
| `workflow_type` | `varchar(40)` | No | Workflow code: authorship-change or copublication. |
| `review_title` | `nvarchar(300)` | No | Invented display title; not a publication title. |
| `review_identifier` | `varchar(30)` | No | Synthetic local review key (SYN-REVIEW-NNNN). |
| `total_authors` | `int` | No | Number of author approval rows for the request, derived from core in the fact. |
| `approved_authors` | `int` | No | Number of author rows currently approved. |
| `outstanding_authors` | `int` | No | Total minus approved; cancellations remain separate terminal states. |
| `current_status` | `varchar(40)` | No | Latest request lifecycle state; must match the last request event. |
| `last_reminder_sent` | `date` | Yes | Most recent reminder day, if any. |
| `reminder_due` | `date` | Yes | Planned next reminder day, if any. |
| `waiting_on` | `varchar(100)` | Yes | Summary actor group awaiting action; blank for finished/cancelled requests. |
| `last_exported_at` | `datetime2(0)` | No | Export timestamp used for calendar-day freshness checks. |

Declared constraints:

- `CONSTRAINT pk_stg_dashboard_exports PRIMARY KEY CLUSTERED (request_id)`

## stg.project_status

[SQL declaration](../sql/azure_sql/01_staging_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `review_identifier` | `varchar(30)` | No | Synthetic local review key (SYN-REVIEW-NNNN). |
| `review_title` | `nvarchar(300)` | No | Invented display title; not a publication title. |
| `project_phase` | `varchar(60)` | No | Synthetic project phase; context-only staging input. |
| `priority` | `varchar(20)` | No | Synthetic priority label (normal or high). |
| `owner_role` | `varchar(60)` | No | Synthetic workflow owner role. |
| `status_as_of` | `date` | No | Reference day of the project-status extract. |
| `next_milestone_due` | `date` | No | Planned project milestone day. |

Declared constraints:

- `CONSTRAINT pk_stg_project_status PRIMARY KEY CLUSTERED (review_identifier)`

## core.workflow_type

[SQL declaration](../sql/azure_sql/02_core_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `workflow_type_code` | `varchar(40)` | No | Workflow code: authorship-change or copublication. |
| `workflow_label` | `varchar(100)` | No | Human-readable workflow label. |
| `is_active` | `bit` | No | Whether the workflow code is active. |

Declared constraints:

- `is_active          bit          NOT NULL CONSTRAINT df_workflow_type_active DEFAULT (1)`
- `CONSTRAINT pk_workflow_type PRIMARY KEY CLUSTERED (workflow_type_code)`

## core.request_status

[SQL declaration](../sql/azure_sql/02_core_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `status_code` | `varchar(40)` | No | Controlled request lifecycle code. |
| `status_label` | `varchar(100)` | No | Human-readable request status label. |
| `terminal_flag` | `bit` | No | Whether a request status closes the open reporting queue. |

Declared constraints:

- `CONSTRAINT pk_request_status PRIMARY KEY CLUSTERED (status_code)`

## core.request

[SQL declaration](../sql/azure_sql/02_core_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `request_id` | `varchar(20)` | No | Stable request natural key; one lifecycle fact per request. |
| `workflow_type_code` | `varchar(40)` | No | Workflow code: authorship-change or copublication. |
| `review_title` | `nvarchar(300)` | No | Invented display title; not a publication title. |
| `review_identifier` | `varchar(30)` | No | Synthetic local review key (SYN-REVIEW-NNNN). |
| `contact_author_name` | `nvarchar(200)` | No | Name of the single author row designated contact author. |
| `created_date` | `date` | No | Request creation day, ISO date in CSV. |
| `due_date` | `date` | No | Request deadline, on or after creation. |
| `current_status` | `varchar(40)` | No | Latest request lifecycle state; must match the last request event. |
| `completion_date` | `date` | Yes | Date of the latest generated/submitted/archived state, when applicable. |
| `dashboard_export_status` | `varchar(40)` | No | Source export state for this request. |
| `inserted_at` | `datetime2(0)` | No | SQL Server ingestion timestamp, default server time. |
| `updated_at` | `datetime2(0)` | No | SQL Server last entity upsert timestamp. |
| `completion_date` | `IS` | Yes | Date of the latest generated/submitted/archived state, when applicable. |

Declared constraints:

- `inserted_at             datetime2(0)   NOT NULL CONSTRAINT df_request_inserted_at DEFAULT (sysdatetime())`
- `updated_at              datetime2(0)   NOT NULL CONSTRAINT df_request_updated_at DEFAULT (sysdatetime())`
- `CONSTRAINT pk_request PRIMARY KEY CLUSTERED (request_id)`
- `CONSTRAINT uq_request_review_identifier UNIQUE (review_identifier)`
- `CONSTRAINT uq_request_workflow UNIQUE (request_id, workflow_type_code)`
- `CONSTRAINT fk_request_workflow_type FOREIGN KEY (workflow_type_code) REFERENCES core.workflow_type(workflow_type_code)`
- `CONSTRAINT fk_request_status FOREIGN KEY (current_status) REFERENCES core.request_status(status_code)`
- `CONSTRAINT ck_request_dates CHECK (due_date >= created_date)`
- `CONSTRAINT ck_request_completion CHECK ( completion_date IS NULL OR completion_date >= created_date )`

## core.author_approval

[SQL declaration](../sql/azure_sql/02_core_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `author_approval_id` | `varchar(30)` | No | Request-specific author approval natural key; not a global person ID. |
| `request_id` | `varchar(20)` | No | Stable request natural key; one lifecycle fact per request. |
| `author_name` | `nvarchar(200)` | No | Invented author display label. |
| `author_role` | `varchar(40)` | No | contact author or co-author; exactly one contact per request. |
| `display_order` | `int` | No | Positive author ordering position within the request. |
| `approval_status` | `varchar(40)` | No | pending, sent, approved, declined, or needs-follow-up. |
| `date_sent` | `date` | Yes | Day approval was requested; blank before sending. |
| `date_approved` | `date` | Yes | Day the author approved; present iff author state is approved. |
| `last_reminder_sent` | `date` | Yes | Most recent reminder day, if any. |
| `reminder_due` | `date` | Yes | Planned next reminder day, if any. |
| `inserted_at` | `datetime2(0)` | No | SQL Server ingestion timestamp, default server time. |
| `updated_at` | `datetime2(0)` | No | SQL Server last entity upsert timestamp. |
| `date_approved` | `IS` | Yes | Day the author approved; present iff author state is approved. |

Declared constraints:

- `inserted_at        datetime2(0)  NOT NULL CONSTRAINT df_author_approval_inserted_at DEFAULT (sysdatetime())`
- `updated_at         datetime2(0)  NOT NULL CONSTRAINT df_author_approval_updated_at DEFAULT (sysdatetime())`
- `CONSTRAINT pk_author_approval PRIMARY KEY CLUSTERED (author_approval_id)`
- `CONSTRAINT fk_author_approval_request FOREIGN KEY (request_id) REFERENCES core.request(request_id)`
- `CONSTRAINT ck_author_status CHECK (approval_status IN ('pending','sent','approved','declined','needs-follow-up'))`
- `CONSTRAINT ck_author_display_order CHECK (display_order > 0)`
- `CONSTRAINT ck_author_approval_dates CHECK ( date_approved IS NULL OR date_sent IS NULL OR date_approved >= date_sent )`

## core.approval_event

[SQL declaration](../sql/azure_sql/02_core_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `event_id` | `varchar(40)` | No | Stable lifecycle or author event natural key. |
| `request_id` | `varchar(20)` | No | Stable request natural key; one lifecycle fact per request. |
| `author_approval_id` | `varchar(30)` | Yes | Request-specific author approval natural key; not a global person ID. |
| `event_type` | `varchar(60)` | No | Request state for request events; author-sent or author-approved for author events. |
| `event_timestamp` | `datetime2(0)` | No | Event time, timezone-naive ISO timestamp (seconds) in the synthetic fixture. |
| `actor_role` | `varchar(60)` | No | Role responsible for the synthetic event. |
| `approval_method` | `varchar(60)` | Yes | Attestation mechanism for approval events, otherwise blank. |
| `comments` | `nvarchar(1000)` | Yes | Generated event annotation; contains no source document text. |
| `inserted_at` | `datetime2(0)` | No | SQL Server ingestion timestamp, default server time. |

Declared constraints:

- `inserted_at        datetime2(0)   NOT NULL CONSTRAINT df_approval_event_inserted_at DEFAULT (sysdatetime())`
- `CONSTRAINT pk_approval_event PRIMARY KEY CLUSTERED (event_id)`
- `CONSTRAINT fk_approval_event_request FOREIGN KEY (request_id) REFERENCES core.request(request_id)`
- `CONSTRAINT fk_approval_event_author FOREIGN KEY (author_approval_id) REFERENCES core.author_approval(author_approval_id)`

## core.reminder_event

[SQL declaration](../sql/azure_sql/02_core_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `reminder_id` | `varchar(40)` | No | Stable reminder natural key. |
| `request_id` | `varchar(20)` | No | Stable request natural key; one lifecycle fact per request. |
| `author_approval_id` | `varchar(30)` | No | Request-specific author approval natural key; not a global person ID. |
| `reminder_type` | `varchar(40)` | No | Synthetic reminder category (follow-up). |
| `sent_at` | `date` | Yes | Reminder send day; no actual message is sent by this project. |
| `sent_by` | `varchar(100)` | No | Synthetic sending actor label. |
| `reminder_number` | `int` | No | Positive ordinal reminder number. |
| `delivery_status` | `varchar(40)` | No | Synthetic delivery state. |
| `next_reminder_due` | `date` | Yes | Planned follow-up day, if any. |
| `inserted_at` | `datetime2(0)` | No | SQL Server ingestion timestamp, default server time. |

Declared constraints:

- `inserted_at        datetime2(0) NOT NULL CONSTRAINT df_reminder_event_inserted_at DEFAULT (sysdatetime())`
- `CONSTRAINT pk_reminder_event PRIMARY KEY CLUSTERED (reminder_id)`
- `CONSTRAINT fk_reminder_event_request FOREIGN KEY (request_id) REFERENCES core.request(request_id)`
- `CONSTRAINT fk_reminder_event_author FOREIGN KEY (author_approval_id) REFERENCES core.author_approval(author_approval_id)`
- `CONSTRAINT ck_reminder_number CHECK (reminder_number > 0)`

## core.generated_document

[SQL declaration](../sql/azure_sql/02_core_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `document_id` | `varchar(30)` | No | Stable generated-document metadata key; no document body is stored. |
| `request_id` | `varchar(20)` | No | Stable request natural key; one lifecycle fact per request. |
| `workflow_type` | `varchar(40)` | No | Workflow code: authorship-change or copublication. |
| `template_version` | `varchar(30)` | No | Synthetic template version label. |
| `generated_at` | `datetime2(0)` | No | Document generation event date in the fixture; SQL supports a timestamp. |
| `generated_by` | `varchar(100)` | No | Synthetic generating actor label. |
| `document_status` | `varchar(40)` | No | Synthetic output state (needs-review or final). |
| `inserted_at` | `datetime2(0)` | No | SQL Server ingestion timestamp, default server time. |

Declared constraints:

- `inserted_at      datetime2(0) NOT NULL CONSTRAINT df_generated_document_inserted_at DEFAULT (sysdatetime())`
- `CONSTRAINT pk_generated_document PRIMARY KEY CLUSTERED (document_id)`
- `CONSTRAINT fk_generated_document_request_workflow FOREIGN KEY (request_id, workflow_type) REFERENCES core.request(request_id, workflow_type_code)`

## mart.dim_date

[SQL declaration](../sql/azure_sql/03_mart_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `date_key` | `int` | No | Surrogate/dimension key; see this table's declared key and foreign-key constraints. |
| `calendar_date` | `date` | No | Calendar day represented by a date-dimension row. |
| `calendar_year` | `int` | No | Four-digit calendar year. |
| `calendar_quarter` | `tinyint` | No | Calendar quarter 1 through 4. |
| `calendar_month` | `tinyint` | No | Calendar month 1 through 12. |
| `month_name` | `varchar(20)` | No | Stable English month label, independent of session language. |
| `day_of_month` | `tinyint` | No | Day of month 1 through 31. |
| `day_of_week_name` | `varchar(20)` | No | Stable English weekday label, independent of session language and DATEFIRST. |
| `is_weekend` | `bit` | No | Weekend indicator derived from a fixed Monday anchor, independent of session settings. |

Declared constraints:

- `CONSTRAINT pk_dim_date PRIMARY KEY CLUSTERED (date_key)`
- `CONSTRAINT uq_dim_date_calendar_date UNIQUE (calendar_date)`

## mart.dim_workflow_type

[SQL declaration](../sql/azure_sql/03_mart_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `workflow_type_key` | `int` | No | Surrogate/dimension key; see this table's declared key and foreign-key constraints. |
| `workflow_type_code` | `varchar(40)` | No | Workflow code: authorship-change or copublication. |
| `workflow_label` | `varchar(100)` | No | Human-readable workflow label. |

Declared constraints:

- `CONSTRAINT pk_dim_workflow_type PRIMARY KEY CLUSTERED (workflow_type_key)`
- `CONSTRAINT uq_dim_workflow_type_code UNIQUE (workflow_type_code)`

## mart.dim_status

[SQL declaration](../sql/azure_sql/03_mart_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `status_key` | `int` | No | Surrogate/dimension key; see this table's declared key and foreign-key constraints. |
| `status_code` | `varchar(40)` | No | Controlled request lifecycle code. |
| `status_label` | `varchar(100)` | No | Human-readable request status label. |
| `terminal_flag` | `bit` | No | Whether a request status closes the open reporting queue. |

Declared constraints:

- `CONSTRAINT pk_dim_status PRIMARY KEY CLUSTERED (status_key)`
- `CONSTRAINT uq_dim_status_code UNIQUE (status_code)`

## mart.dim_review

[SQL declaration](../sql/azure_sql/03_mart_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `review_key` | `int` | No | Surrogate/dimension key; see this table's declared key and foreign-key constraints. |
| `review_identifier` | `varchar(30)` | No | Synthetic local review key (SYN-REVIEW-NNNN). |
| `review_title` | `nvarchar(300)` | No | Invented display title; not a publication title. |
| `first_request_id` | `varchar(20)` | No | First request natural key associated with the review dimension. |

Declared constraints:

- `CONSTRAINT pk_dim_review PRIMARY KEY CLUSTERED (review_key)`
- `CONSTRAINT uq_dim_review_identifier UNIQUE (review_identifier)`

## mart.dim_author

[SQL declaration](../sql/azure_sql/03_mart_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `author_key` | `int` | No | Surrogate/dimension key; see this table's declared key and foreign-key constraints. |
| `author_approval_id` | `varchar(30)` | No | Request-specific author approval natural key; not a global person ID. |
| `author_name` | `nvarchar(200)` | No | Invented author display label. |
| `author_role` | `varchar(40)` | No | contact author or co-author; exactly one contact per request. |
| `approval_status` | `varchar(40)` | No | pending, sent, approved, declined, or needs-follow-up. |
| `effective_start_date` | `date` | No | Inclusive load day starting this author version, not source date_sent. |
| `effective_end_date` | `date` | Yes | Inclusive last day of a closed version; NULL for the current version. |
| `is_current` | `bit` | No | 1 for the single open author version; same-day changes coalesce. |
| `effective_end_date` | `IS` | Yes | Inclusive last day of a closed version; NULL for the current version. |

Declared constraints:

- `CONSTRAINT pk_dim_author PRIMARY KEY CLUSTERED (author_key)`
- `CONSTRAINT uq_dim_author_scd UNIQUE (author_approval_id, effective_start_date)`
- `CONSTRAINT ck_dim_author_scd_dates CHECK ( effective_end_date IS NULL OR effective_end_date >= effective_start_date )`

## mart.fact_request_lifecycle

[SQL declaration](../sql/azure_sql/03_mart_tables.sql)

| Column | SQL type | Nullable | Meaning |
| --- | --- | --- | --- |
| `request_id` | `varchar(20)` | No | Stable request natural key; one lifecycle fact per request. |
| `review_key` | `int` | No | Surrogate/dimension key; see this table's declared key and foreign-key constraints. |
| `workflow_type_key` | `int` | No | Surrogate/dimension key; see this table's declared key and foreign-key constraints. |
| `current_status_key` | `int` | No | Surrogate/dimension key; see this table's declared key and foreign-key constraints. |
| `created_date_key` | `int` | No | Surrogate/dimension key; see this table's declared key and foreign-key constraints. |
| `due_date_key` | `int` | No | Surrogate/dimension key; see this table's declared key and foreign-key constraints. |
| `completion_date_key` | `int` | Yes | Surrogate/dimension key; see this table's declared key and foreign-key constraints. |
| `cycle_time_days` | `int` | Yes | Calendar days from request creation to completion; NULL if incomplete. |
| `total_authors` | `int` | No | Number of author approval rows for the request, derived from core in the fact. |
| `approved_authors` | `int` | No | Number of author rows currently approved. |
| `outstanding_authors` | `int` | No | Total minus approved; cancellations remain separate terminal states. |

Declared constraints:

- `CONSTRAINT pk_fact_request_lifecycle PRIMARY KEY CLUSTERED (request_id)`
- `CONSTRAINT fk_fact_lifecycle_review FOREIGN KEY (review_key) REFERENCES mart.dim_review(review_key)`
- `CONSTRAINT fk_fact_lifecycle_workflow FOREIGN KEY (workflow_type_key) REFERENCES mart.dim_workflow_type(workflow_type_key)`
- `CONSTRAINT fk_fact_lifecycle_status FOREIGN KEY (current_status_key) REFERENCES mart.dim_status(status_key)`
- `CONSTRAINT fk_fact_lifecycle_created_date FOREIGN KEY (created_date_key) REFERENCES mart.dim_date(date_key)`
- `CONSTRAINT fk_fact_lifecycle_due_date FOREIGN KEY (due_date_key) REFERENCES mart.dim_date(date_key)`
- `CONSTRAINT fk_fact_lifecycle_completion_date FOREIGN KEY (completion_date_key) REFERENCES mart.dim_date(date_key)`

`mart.dim_author` also has a filtered unique index allowing one current row per author.
The linked SQL declarations are the source of these column and constraint definitions.
